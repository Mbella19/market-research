# secrets — never commit
.env

# local runtime data — never commit
data/
*.db
*.db-shm
*.db-wal

# frontend — intentionally excluded from the public repo
web/

# dependencies, tooling, OS noise
node_modules/
.claude/
CLAUDE.md
graphify-out/
.DS_Store
*.log
nohup.out
